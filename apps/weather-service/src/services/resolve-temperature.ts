import type pg from "pg";
import { getGeographyContext, type FetchLike, type GeographyContext } from "../clients/geography-client.js";
import { getModelTemperature } from "../clients/weather-model-client.js";
import { selectStationObservation, type StationMeasurement } from "../policies/station-selection-policy.js";
import { loadNearbyStationMeasurements } from "../repositories/station-observation-repository.js";
import type { WeatherConfig } from "../config.js";
export interface TemperatureDependencies { geography?: (lat: number, lon: number, accuracy: number | undefined) => Promise<GeographyContext>; model?: (lat: number, lon: number) => Promise<{ valueCelsius: number; referenceTime: string }>; observations?: (lat: number, lon: number) => Promise<StationMeasurement[]>; now?: () => Date }
export async function resolveTemperature(config: WeatherConfig, pool: pg.Pool | null, latitude: number, longitude: number, horizontalAccuracyMeters: number | undefined, fetchImpl: FetchLike, dependencies: TemperatureDependencies = {}) { const now = dependencies.now?.() ?? new Date(); const geography = dependencies.geography ?? ((lat, lon, accuracy) => getGeographyContext(config.geographyServiceUrl, lat, lon, accuracy, config.geographyTimeoutMs, fetchImpl)); const model = dependencies.model ?? ((lat, lon) => getModelTemperature(config.weatherModelUrl, lat, lon, config.weatherModelTimeoutMs, fetchImpl)); const observations = dependencies.observations ?? ((lat, lon) => pool ? loadNearbyStationMeasurements(pool, lat, lon) : Promise.reject(new Error("base non configurée"))); const geographyResult = await geography(latitude, longitude, horizontalAccuracyMeters); const [measurementResult, modelResult] = await Promise.allSettled([observations(geographyResult.latitude, geographyResult.longitude), model(geographyResult.latitude, geographyResult.longitude)]); const measurements = measurementResult.status === "fulfilled" ? measurementResult.value : []; const decision = selectStationObservation({ latitude: geographyResult.latitude, longitude: geographyResult.longitude, altitudeM: geographyResult.altitudeMeters }, measurements, now); const selected = decision.selected; const modelValue = modelResult.status === "fulfilled" ? modelResult.value : null; if (!selected && !modelValue) { const error = new Error("Aucune température exploitable"); (error as Error & { code?: string }).code = "WEATHER_NOT_AVAILABLE"; throw error; } const useStation = selected !== null; const referenceTime = useStation ? selected.observedAt : modelValue!.referenceTime; const source = useStation ? { id: selected.station.reseau === "meteofrance" ? "meteofrance-dpobs" : "infoclimat-static", provider: selected.station.reseau === "meteofrance" ? "Météo-France" : "Infoclimat", product: selected.station.reseau === "meteofrance" ? "DPObs" : "StatIC", license: selected.station.licence } : { id: "meteofrance-arome", provider: "Météo-France", product: "AROME via Open-Meteo", license: "Licence fournisseur" }; return { location: { latitude: geographyResult.latitude, longitude: geographyResult.longitude, ...(geographyResult.horizontalAccuracyMeters !== undefined ? { horizontalAccuracyMeters: geographyResult.horizontalAccuracyMeters } : {}), altitudeMeters: geographyResult.altitudeMeters }, temperature: { valueCelsius: useStation ? Math.round(selected.temperatureC * 10) / 10 : modelValue!.valueCelsius, nature: useStation ? "station_observation" as const : "model_at_point" as const, referenceTime, retrievedAt: now.toISOString(), ageMinutes: useStation ? selected.ageMinutes : null, stale: useStation ? selected.stale : false, quality: useStation && selected.stale ? "stale" as const : "good" as const }, method: { id: "meteo-v2-temperature" as const, version: "1" as const, stationSelectionPolicyVersion: "1" as const }, stationSelection: { status: decision.status, reasonCode: decision.reasonCode, evaluatedCandidates: decision.evaluatedCandidates, eligibleCandidates: decision.eligibleCandidates, selectedStation: selected ? { id: selected.station.id, name: selected.station.nom, network: selected.station.reseau, altitudeMeters: selected.station.altitudeM, distanceKilometers: selected.distanceKm, altitudeDifferenceMeters: selected.altitudeDifferenceM, ageMinutes: selected.ageMinutes, selectionScore: selected.selectionScore } : null }, provenance: { source }, degraded: !useStation || measurementResult.status === "rejected", unavailableSources: measurementResult.status === "rejected" ? ["station_observations"] : [], }; }
